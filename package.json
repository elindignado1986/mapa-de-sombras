{
  "name": "ciudad-jardin-mapa",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.cjs",
    "dev": "node scripts/build.cjs && node scripts/serve.cjs publicar",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/check-app.cjs",
    "data:fetch": "node scripts/fetch-arba.cjs",
    "data:build": "node scripts/build-map-data.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "0.28.2"
  },
  "dependencies": {
    "earcut": "^3.2.3",
    "fflate": "^0.8.3",
    "polygon-clipping": "^0.15.7",
    "proj4": "^2.22.0",
    "qrcode": "^1.5.4",
    "shpjs": "^6.2.0"
  }
}
