const fs=require('node:fs');
(async()=>{const url='https://geo.arba.gov.ar/datoabierto/datos/117/070121',r=await fetch(url,{signal:AbortSignal.timeout(30000)});if(!r.ok)throw Error('ARBA HTTP '+r.status);const zip=require('./archive.cjs').shapeZip(Buffer.from(await r.arrayBuffer())),{default:shp}=await import('shpjs'),fc=await shp(zip);if(Array.isArray(fc)||fc.features.length!==1)throw Error('Revisar capa Departamento: se esperaba un límite municipal');fs.mkdirSync('data/context',{recursive:true});fs.writeFileSync('data/context/tres-de-febrero-boundary.json',JSON.stringify({type:'Feature',properties:{name:'Tres de Febrero',source:'ARBA',sourceURL:url,downloadedAt:new Date().toISOString()},geometry:fc.features[0].geometry}));console.log('Límite municipal ARBA preparado');})().catch(e=>{console.error(e.message);process.exitCode=1});
